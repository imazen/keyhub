@model KeyHub.Web.ViewModels.User.UserEditViewModel

@{
    ViewBag.ItemName = "Account";
    ViewBag.Title = "Edit";
}

<h2>@ViewBag.Title <span>@ViewBag.ItemName</span></h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary()

    <fieldset>
        <legend>UserEditViewItem</legend>

        @Html.HiddenFor(model => model.User.UserId)

        <div class="editor-label">
            @Html.LabelFor(model => model.User.UserName)
        </div>
        <div class="editor-field">
            @Html.HiddenFor(model => model.User.UserName)
            @Html.DisplayFor(model => model.User.UserName)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.User.LastActivityDate)
        </div>
        <div class="editor-field">
            @Html.HiddenFor(model => model.User.LastActivityDate)
            @Html.DisplayFor(model => model.User.LastActivityDate)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.User.Email)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.User.Email, new {autocomplete="off"})
            @Html.ValidationMessageFor(model => model.User.Email)
        </div>      

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to account details", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
